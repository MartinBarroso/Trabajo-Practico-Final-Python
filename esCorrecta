def esCorrecta(palabraUsuario, letra, item, items, listaDeTodo):
    if len(palabraUsuario)>0:
        pal = items.index(item)
        if palabraUsuario[0]==letra and palabraUsuario in listaDeTodo[pal]:
            return 10
        else:
            return -5
    else:
        return -5
